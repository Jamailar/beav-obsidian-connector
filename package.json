{
  "name": "@beav/obsidian-connector",
  "version": "0.1.0",
  "description": "A local Obsidian connector for Beav knowledge, search, and authoring workflows.",
  "license": "MIT",
  "private": false,
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs development",
    "check": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check && npm run build && npm run test"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "esbuild": "^0.25.4",
    "obsidian": "^1.11.4",
    "typescript": "^5.8.3"
  }
}
